################################################################################
# OPEN SOURCE MODULE
# Remove the comment if you changed the build scripts!
################################################################################

machine:
  timezone: Europe/Berlin

  node:
    version: 8.9.4

dependencies:
  pre:
    - npm install -g roboter-cli

test:
  override:
    - npm run bot
